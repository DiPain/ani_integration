import 'package:flutter/material.dart';

class EventData{
  String title,des, date, loca, price,time, moreAddress;
  Color color;
  
  EventData(this.title, this.des, this.date, this.loca, this.price, this.color,this.time);
}